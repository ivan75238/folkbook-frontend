import React from 'react';
import Paginator from "./Paginator";
import 'jsdom-global/register';

const props = {
    countOnPage: 10,
    currentPage: 1,
    countItems: 200,
    onChangeCurrentPage: (page) => {}
};

describe("Тесты компонента Paginator", () => {
    describe("тесты оотрисовки", () => {

    })
});
